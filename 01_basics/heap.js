//Heap

let user = {
    email:"arif02@gmai.com",
    user_name:"Arif"
}

let usertwo=user;

usertwo.email="sandhi21@gmail.com";


// in heap we sotre values and give output using refrense and HEAP(Non-premitive)

console.log(user.email);
console.log(usertwo.email);

//if i change any value of any of the objects then that value change for every objects ..
// but in stack system makes a copy and do not change real value.

